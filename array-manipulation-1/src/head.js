/* exported head */
// pseudocde
// Identify the first item of each list
// Return that first item using it's position in the list (The first item in the list should always begin at position at 0 as opposed to 1)

function head(array) {
  return array[0];
}

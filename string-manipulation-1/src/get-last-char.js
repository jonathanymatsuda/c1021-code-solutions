/* exported getLastChar */
// pseudocode
// - Identify how long the word is to start with.
// - Once the word length is identified, identify where the last character is.
// - Return the last character.
function getLastChar(string) {
  return string[string.length - 1];
}
